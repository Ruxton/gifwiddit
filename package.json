{
  "name": "gifwiddit-ruxton",
  "version": "0.2.5",
  "description": "Electro n Fool.",
  "dependencies": {
    "menubar": "*"
  },
  "devDependencies": {
    "electron-packager": "^7.0.0",
    "electron-prebuilt": "^1.3.3"
  },
  "main": "index.js",
  "scripts": {
    "build": "./node_modules/.bin/electron-packager . Gifwiddit --platform=darwin --arch=x64 --version=1.3.3 --app-version=0.2.4 --cache=$HOME/.gifwiddit --asar --overwrite",
    "start": "./node_modules/.bin/electron . --disable-gpu"
  }
}
